# BLUEPRINT — Engineering Skills Pack

> **وضعیت: FINAL — تأییدشده**
>
> از این پس اجرا طبق Roadmap (بخش 13) پیش می‌رود، گام به گام، با دروازهٔ تأیید در هر گام.

## فهرست

| بخش | موضوع |
|---|---|
| [Context](#context) | چرا این پروژه |
| [1](#1-research-first-development) | Research-First Development |
| [2](#2-product-vision) | Product Vision |
| [3](#3-phase-1-workflow) | Phase 1 Workflow |
| [4](#4-skill-architecture) | Skill Architecture |
| [5](#5-trust-model) | Trust Model |
| [6](#6-findings-schema) | Findings Schema |
| [7](#7-domain-ownership) | Domain Ownership |
| [8](#8-remediation-workflow) | Remediation Workflow |
| [9](#9-evaluation) | Evaluation |
| [10](#10-repository-structure) | Repository Structure |
| [11](#11-installation) | Installation |
| [12](#12-non-goals) | Non-Goals |
| [13](#13-roadmap) | Roadmap |
| [14](#14-definition-of-done) | Definition of Done |
| [15](#15-future-enhancements) | Future Enhancements |
| [خلاصه](#خلاصهٔ-تغییرات-نسبت-به-نسخهٔ-قبل) | خلاصهٔ تغییرات نسبت به نسخهٔ قبل |

---

## Context

پروژهٔ قبلی با نام AI Engineering OS با هدف ساده‌ای شروع شد و به orchestration،
debate loop، state machine و dashboard تبدیل شد.

دو درس گرفتیم و هر دو در این سند سخت‌گیرانه اعمال شده‌اند:

**۱. مشکل، نبودِ زیرساخت نبود. نبودِ تخصص بود.**
یک agent با ده لایه هماهنگی، اگر نداند یک Senior Security Engineer واقعاً به چه چیزی
نگاه می‌کند، فقط با سرعت بیشتری چیز بی‌ربط تولید می‌کند.

**۲. هر چیزی که «شاید بعداً لازم شود» بسازیم، همان چیزی است که پروژه را می‌کشد.**

پس این پروژه دو ستون دارد و بس:

```text
    ستون اول                          ستون دوم
    RESEARCH-FIRST                    MVP کوچک و بی‌رحمانه
    هیچ Skill از حافظهٔ مدل            سه Skill، بعد توقف
    ساخته نمی‌شود                      تا اثبات ارزش روی پروژهٔ واقعی
```

بخش 1 این سند مهم‌ترین بخش است. بقیه جزئیات اجرایی است.

---

## 1. Research-First Development

### 1.1 قانون بنیادین

> **هیچ Skillی مستقیماً از حافظهٔ مدل تولید نمی‌شود.**

Claude دربارهٔ امنیت، دیتابیس و معماری «چیزهایی می‌داند».
آن دانش، میانگینِ مبهمِ اینترنت است: قدیمی، سطحی، و پر از توصیه‌هایی که ده سال پیش درست بودند.

یک Skill که از آن حافظه نوشته شود، یک چک‌لیست عمومی تولید می‌کند که همه‌جا صدق می‌کند
و هیچ‌جا مفید نیست. دقیقاً همان چیزی که نمی‌خواهیم.

پس قبل از هر Skill، **حوزه را مطالعه می‌کنیم**، نه Skillهای مشابه را.

### 1.2 سؤال محوری هر Research

برای هر حوزه، تحقیق باید به این سؤال جواب دهد:

> **یک متخصص Senior واقعیِ همین حوزه، امروز، وقتی این کد را باز می‌کند،
> دقیقاً دنبال چه می‌گردد — و چرا؟**

نه «چه چیزهایی خوب است». نه «best practiceها چیست».

بلکه: این آدم در عمل چه کار می‌کند، به چه ترتیبی، و چه چیزی را عمداً نادیده می‌گیرد.

#### تفاوت حافظه با تحقیق

**دربارهٔ امنیت**

- حافظهٔ مدل می‌گوید: «SQL Injection را بررسی کن»
- تحقیق باید کشف کند: امروز اکثر ORMها این را حل کرده‌اند؛ یک Senior AppSec وقتش را
  روی مرزهای اعتماد، منطق مجوزدهی و SSRF می‌گذارد — نه روی رشته‌های SQL

**دربارهٔ دیتابیس**

- حافظهٔ مدل می‌گوید: «ایندکس بگذار»
- تحقیق باید کشف کند: یک Senior DB Engineer اول به الگوی نوشتن و خواندن و ایمنی
  migration نگاه می‌کند، چون ایندکس اشتباه بدتر از نبودِ ایندکس است

**دربارهٔ تست**

- حافظهٔ مدل می‌گوید: «تست بنویس»
- تحقیق باید کشف کند: تست‌های زیاد با پوشش بالا که هیچ رفتاری را واقعاً تأیید نمی‌کنند،
  رایج‌ترین شکست است

این تفاوت، تفاوت بین یک Skill بی‌مصرف و یک Skill واقعی است.

> **توجه**
>
> نمونه‌های بالا فقط برای نشان‌دادن *شکل* تفاوت‌اند. طرفِ «تحقیق باید کشف کند» هنوز
> **فرضیه** است، نه یافته. خودِ همین‌ها هم باید در Research تأیید یا رد شوند — وگرنه
> دقیقاً همان اشتباهی را کرده‌ایم که این بخش می‌خواهد جلویش را بگیرد.

### 1.3 چرخهٔ Research برای هر حوزه

```text
  1  Understand the Domain
       حوزه دقیقاً چیست؟ مرزش کجاست؟ چه کسی این کار را انجام می‌دهد؟
                    ↓
  2  Study Official Standards
       استاندارد رسمی و به‌روزِ حوزه، و بررسی اینکه نسخهٔ جدیدتری هست یا نه
                    ↓
  3  Study Authoritative Technical Sources
       مستندات رسمی ابزارها، نوشتهٔ نگهدارندگان، سازمان‌های فنی شناخته‌شده
                    ↓
  4  Study High-Quality Repositories / Existing Skills
       فقط به‌عنوان یکی از منابع — نه منبع اصلی
                    ↓
  5  Compare Different Approaches
       جاهایی که منابع معتبر با هم اختلاف دارند را پیدا کن و ثبت کن
                    ↓
  6  Identify Blind Spots and False Positives
       حیاتی‌ترین گام — پایین توضیح داده شده
                    ↓
  7  Extract Current Best Practices
       «current» یعنی امروز، نه آنچه پنج سال پیش درست بود
                    ↓
  8  Design Our Review Methodology
       روال بازرسی خودمان: با چه ترتیبی، با چه اولویتی، با چه شواهدی
                    ↓
  9  Build the Skill
                    ↓
 10  Evaluate
```

#### چرا گام 6 حیاتی‌ترین است

هر کسی می‌تواند چک‌لیست جمع کند.
چیزی که یک Skill را از یک چک‌لیست جدا می‌کند این است که **بداند کجا اشتباه می‌کند**.

Research باید صراحتاً سه چیز را ثبت کند:

- **False Positiveهای رایج** — چیزهایی که *شبیه* مشکل‌اند ولی نیستند.
  نمونه از شکلِ چنین موردی: کدی که خطرناک به نظر می‌رسد ولی ورودی‌اش هرگز از کاربر نمی‌آید.
  Skill باید بداند قبل از گزارش، این را چک کند.
- **Blind Spotها** — چیزهایی که یک بازرس (انسان یا مدل) معمولاً از دستش می‌دهد،
  چون در کد دیده نمی‌شوند: تنظیمات محیط، ترتیب اجرا، رفتار زیر بار.
- **توصیه‌های منسوخ** — چیزهایی که هنوز در اینترنت هستند ولی دیگر درست نیستند.
  Skill باید صراحتاً بگوید این‌ها را گزارش نکند.

بدون این گام، Skill پرِ نویز می‌شود و بعد از دو بار استفاده کنارش می‌گذاری.

### 1.4 سلسله‌مراتب منابع

منابع به این ترتیب **وزنِ پیش‌فرضِ اعتبار** دارند:

```text
1. Official standards                    ← بالاترین وزن
2. Official documentation
3. Recognized technical organizations
4. Maintainer documentation
5. High-quality engineering publications
6. Academic / research material
7. High-quality GitHub repositories
8. Existing Claude / Agent Skills        ← پایین‌ترین وزن
```

#### این سلسله‌مراتب وزن است، نه قانون مطلق

رده، نقطهٔ **شروع** قضاوت است، نه پایان آن.

یک استاندارد رسمیِ ۲۰۱۸ می‌تواند در یک مورد مشخص از مستندات امسالِ نگهدارندهٔ همان
کتابخانه بی‌ربط‌تر باشد.

وقتی دو منبع معتبر با هم اختلاف دارند، هر پنج محور بررسی می‌شود:

| محور | سؤال |
|---|---|
| `scope` | هر منبع دربارهٔ چه چیزی حرف می‌زند؟ آیا واقعاً دربارهٔ یک چیز حرف می‌زنند؟ |
| `applicability` | کدام‌یک به وضعیت *ما* مربوط است؟ |
| تاریخ | کدام جدیدتر است؟ آیا قدیمی‌تر منسوخ شده یا هنوز معتبر است؟ |
| `specificity` | کدام دقیقاً دربارهٔ همین مورد است و کدام کلی حرف می‌زند؟ |
| `authority` | کدام‌یک در *این موضوع خاص* مرجع است؟ نه به‌طور کلی |

خودِ اختلاف و تصمیم نهایی در بخش 11 فایل `research/<skill-name>.md` ثبت می‌شود،
با ذکر اینکه چرا یک طرف انتخاب شد.

اختلافِ ثبت‌نشده یعنی قضاوتِ انجام‌نشده.

**GitHub یکی از منابع است، نه منبع اصلی.**
Skillهای موجود در پایین‌ترین رده هستند — از آن‌ها *ساختار* و *ایده* می‌گیریم،
نه *محتوای فنی*.

**بررسی تازگی اجباری است.** برای هر استاندارد یا منبع: آیا نسخهٔ جدیدتری منتشر شده؟
آیا چیزی جایگزینش شده؟ آیا این توصیه هنوز معتبر است؟
اگر تاریخ منبع را نمی‌دانیم، نباید استفاده شود.

### 1.5 ارزیابی repository — ستاره معیار نیست

هر repositoryای که به آن استناد می‌کنیم باید روی این ده محور بررسی شود:

- **latest meaningful commit** — آخرین commit *واقعی*، نه ویرایش `README`
- **contributor activity** — چند نفر؟ یا یک نفر که دو سال است غیبش زده؟
- **issue / PR activity** — آیا کسی جواب می‌دهد؟ issueها باز مانده‌اند؟
- **reputation** — نویسنده کیست؟ سابقه‌اش در این حوزه چیست؟
- **documentation quality** — توضیح داده که *چرا*، یا فقط *چه*؟
- **license** — آیا اصلاً اجازهٔ برداشت داریم؟ این را **قبل از هر کپی‌برداری** چک کن
- **real-world adoption** — کسی واقعاً استفاده می‌کند یا فقط ستاره خورده؟
- **technical depth** — عمق دارد یا فهرست بی‌جان است؟
- **AI-generated یا سطحی؟** — متن پرطمطراق و توخالی، مثال‌های عمومی، بدون درد واقعی
- **آیا توصیه‌هایش هنوز معتبرند؟** — یا مربوط به دورهٔ دیگری‌اند؟

دو محور آخر بیشترین کاربرد را دارند.
مخازن زیادی هستند که ستاره دارند و محتوایشان متنِ تولیدشدهٔ بی‌عمق است.

اگر repository در این دو محور رد شد، در Research به‌عنوان «بررسی و رد شد» ثبت می‌شود.
این خودش یک یافتهٔ ارزشمند است.

### 1.6 Research Artifact — اجباری

قبل از نوشتن حتی یک خط از هر Skill، این فایل باید وجود داشته باشد:

```text
research/<skill-name>.md
```

قالب کامل پانزده بخش دارد. میزان اجباری‌بودن به نوع Skill بستگی دارد (بخش 1.6.1):

```markdown
# Research — <skill-name>

## 1. Domain Definition
حوزه دقیقاً چیست؟ مرزش کجاست؟ چه چیزی جزو آن نیست؟

## 2. What a Senior Specialist Reviews
یک متخصص واقعی در عمل چه کار می‌کند، به چه ترتیبی، با چه اولویتی

## 3. Official Standards
استاندارد رسمی + نسخه + تاریخ + تأیید اینکه جدیدترین است

## 4. Authoritative Sources
مستندات رسمی، سازمان‌های فنی، نوشتهٔ نگهدارندگان

## 5. Important Review Dimensions
محورهای بازرسی — هر کدام با ارجاع به منبعی که از آن آمده

## 6. Common Failure Modes
در واقعیت این حوزه چطور خراب می‌شود

## 7. Common False Positives
چیزهایی که شبیه مشکل‌اند و نیستند + چطور تشخیصشان دهیم

## 8. Common Blind Spots
چیزهایی که معمولاً از دست می‌روند + چرا

## 9. Existing Approaches
روش‌های موجود برای بازرسی این حوزه

## 10. Relevant GitHub Repositories
هر کدام با نتیجهٔ ارزیابی ده‌محوریِ بخش 1.5 — شامل ردشده‌ها

## 11. Comparison
جاهایی که منابع با هم اختلاف دارند + تحلیل ما

## 12. What We Adopt
چه چیزی برمی‌داریم و از کدام منبع

## 13. What We Reject
چه چیزی را عمداً کنار می‌گذاریم و چرا — این بخش به اندازهٔ بخش 12 مهم است

## 14. Final Skill Design Decisions
از تحقیق به طراحی: Skill چه ساختاری خواهد داشت و چرا

## 15. Sources
هر منبع با لینک، تاریخ دسترسی، و رده‌اش در سلسله‌مراتب 1.4
```

#### 1.6.1 قالب بر اساس نوع Skill

قالب پانزده‌بخشی برای **Domain Reviewer**ها طراحی شده — جایی که حوزهٔ تخصصیِ بیرونی،
استاندارد رسمی و ادبیات مستقل وجود دارد.

اما `project-understanding` و `remediation-planner` از این جنس نیستند.

«استاندارد رسمیِ فهمیدن یک پروژه» وجود ندارد. اجبار به پرکردن بخش 3 برای آن‌ها،
Claude را وادار می‌کند چیزی از خودش بسازد — یعنی دقیقاً همان کاری که این کل بخش
می‌خواهد جلویش را بگیرد.

| نوع | Skillها | قالب |
|---|---|---|
| Domain Reviewer | `security-review`، `database-review`، … | هر پانزده بخش اجباری |
| Mapper | `project-understanding` | فقط بخش‌های مرتبط |
| Synthesizer | `remediation-planner` | فقط بخش‌های مرتبط |

##### قانون حذف

> **هیچ بخشی صرفاً برای پرکردن قالب تولید نمی‌شود.**
>
> بخشی که محتوای واقعی ندارد، حذف می‌شود — نه اینکه با متن عمومی پر شود.
> و برای هر بخش حذف‌شده، دلیل حذف در همان Research نوشته می‌شود.

در انتهای هر Research غیر-Reviewer یک بخش کوتاه اضافه می‌شود:

```markdown
## Omitted Sections
- بخش 3 (Official Standards) — حذف شد: برای این حوزه استاندارد رسمی وجود ندارد.
  به جایش از <منبع> استفاده شد.
- بخش 7 (Common False Positives) — حذف شد: این Skill finding تولید نمی‌کند،
  پس مفهوم false positive برایش تعریف نشده.
```

بخش حذف‌شده با دلیل، یک تصمیم است. بخش پرشده با متن توخالی، یک دروغ است.
دومی بدتر است، چون تشخیصش سخت‌تر است.

> **نکته**
>
> جدولِ بالا نمونه است، نه فهرست از پیش تعیین‌شده. اینکه دقیقاً کدام بخش‌ها برای
> `project-understanding` مرتبط‌اند، در خودِ آن Research تصمیم گرفته و توجیه می‌شود —
> نه اینجا و نه از قبل.

### 1.7 Research Quality Gate

Research ضعیف یعنی Skill ساخته نمی‌شود.

**برای هر Skill، هر سه نوع:**

- ❌ یک تصمیم طراحی وجود دارد که به هیچ منبعی وصل نیست، یعنی از حافظهٔ مدل آمده
- ❌ بخش `What We Reject` خالی است — یعنی هیچ قضاوتی نشده، فقط جمع‌آوری شده
- ❌ تازگی منابع بررسی نشده
- ❌ فقط از Skillها و مخازن GitHub تغذیه شده، یعنی ردهٔ 7 و 8
- ❌ اختلاف بین منابع پیدا شده ولی تحلیل و ثبت نشده
- ❌ بخشی حذف شده ولی دلیل حذفش نوشته نشده
- ❌ بخشی با متن عمومی و توخالی پر شده تا قالب کامل به نظر برسد

**فقط برای Domain Reviewerها، علاوه بر بالا:**

- ❌ هیچ منبع ردهٔ 1 تا 3 ندارد، یعنی نه استاندارد نه مستندات رسمی
- ❌ بخش `Common False Positives` خالی یا سطحی است
- ❌ بخش `Common Blind Spots` خالی است

**تو Research را می‌خوانی و تأیید می‌کنی. تا آن تأیید، Skill نوشته نمی‌شود.**

### 1.8 روش انجام Research

- جستجو و خواندن از طریق وب — بدون clone، بدون اجرا، بدون نصب
- فایل‌های Skillهای دیگر به‌صورت خام از وب خوانده می‌شوند
- لایسنس هر منبع قبل از هرگونه برداشت متن بررسی می‌شود
- Research در همان جلسهٔ Claude Code انجام می‌شود، بدون ابزار جدید

---

## 2. Product Vision

### 2.1 چه می‌سازیم

سه پوشهٔ Markdown که به Claude Code یاد می‌دهند مثل یک متخصص Senior رفتار کند
و خروجی ساختاریافته و مبتنی بر شواهد بدهد.

| Skill | نقش |
|---|---|
| `project-understanding` | نقشهٔ پروژه — پیش‌نیاز بقیه |
| `security-review` | بازرسی امنیتی، تولید findings |
| `remediation-planner` | ادغام و اولویت‌بندی findings |

همین. سه تا.

### 2.2 چه نمی‌سازیم

هیچ کدِ اجرایی‌ای که «کار را انجام دهد». Claude Code خودش موتور است.

ما فقط تخصص و قرارداد خروجی می‌نویسیم. فهرست کامل در بخش 12.

### 2.3 تجربهٔ کاربر

```text
cd ~/my-project
claude

> «طبق skill امنیت، این پروژه را بررسی کن و فقط گزارش بده»

  [Claude ابتدا project-map را می‌سازد یا می‌خواند، بعد بازرسی می‌کند]

  ✓ .audit/security-findings.json نوشته شد

  خلاصه:
  ۳ مورد verified پیدا شد — یکی high، دو تا medium.
  مهم‌ترین: مسیر فایل از ورودی کاربر بدون نرمال‌سازی — src/api/files.ts:42
  ۲ مورد هم نیاز به بررسی دستی دارند (unverified).
  ۶۸ فایل بررسی شد، پوشهٔ migrations قدیمی بررسی نشد.
```

خلاصهٔ فارسی در ترمینال نمایش داده می‌شود. دادهٔ ساختاریافته در فایل می‌ماند.

---

## 3. Phase 1 Workflow

نسخهٔ اول **فقط** این است:

```text
   ┌─────────────────────────┐
   │  PROJECT UNDERSTANDING  │  →  .audit/project-map.md
   └───────────┬─────────────┘
               ↓
   ┌─────────────────────────┐
   │     SECURITY REVIEW     │  →  .audit/security-findings.json
   └───────────┬─────────────┘
               ↓
   ┌─────────────────────────┐
   │   REMEDIATION PLANNER   │  →  .audit/remediation-plan.md
   └───────────┬─────────────┘
               ↓
   ┌─────────────────────────┐
   │  NORMAL CLAUDE CODE FIX │  یک finding، یک تست، یک commit
   └───────────┬─────────────┘   بدون Skill جدید
               ↓
   ┌─────────────────────────┐
   │       RE-REVIEW         │  همان security-review، روی تغییرات
   └───────────┬─────────────┘
               ↓
         ⛔  توقف کامل
```

**بعد از این، ساخت متوقف می‌شود.** روی یک پروژهٔ واقعی امتحان می‌کنیم:

- اگر مفید بود → Skill بعدی
- اگر نبود → Blueprint اصلاح می‌شود، نه اینکه ۹ Skill دیگر هم ساخته شود

فایل `project-map.md` یک بار ساخته می‌شود و می‌ماند.
`security-review` اول آن را می‌خواند تا بداند کدام ورودی از اینترنت می‌آید و کدام داخلی
است — بدون این، همه چیز `high` می‌شود.

### 3.1 تازگی project-map

نقشه‌ای که با کد فعلی نمی‌خواند، بدتر از نداشتن نقشه است:
Reviewer را با اطمینان به سمت اشتباه می‌برد.

فایل `project-map.md` در سرآمدش این دو فیلد را دارد:

```markdown
---
repo_commit: a3f91c2
generated_at: 2026-08-16
---
```

#### نقشه در Phase 1 اجباری است

اگر `project-map.md` وجود نداشت، Reviewer متوقف می‌شود، هیچ خروجی نمی‌نویسد،
و به کاربر می‌گوید اول `project-understanding` اجرا شود.

بازرسی بدون نقشه یعنی ندانستن مرزهای اعتماد — و گزارشی که همه‌چیز را `high` می‌کند.

#### اگر نقشه وجود داشت

هر Reviewer این سه گام را انجام می‌دهد:

```text
1.  repo_commit را با HEAD فعلی مقایسه کن
       یکی بود؟ → نقشه تازه است، استفاده کن ✓

2.  فرق داشت؟ تغییرات بین آن دو commit را نگاه کن (git diff --stat)

3.  قضاوت کن:
       فقط تغییرات معمولی داخل فایل‌ها؟   → استفاده کن، ولی در coverage ذکر کن
       تغییر ساختاری یا معماری؟            → به کاربر بگو نقشه باید refresh شود
```

«تغییر ساختاری یا معماری» یعنی چیزی که خودِ نقشه را غلط می‌کند:

- فایل یا پوشهٔ جدید در سطح بالا
- وابستگی جدید
- تغییر در نقاط ورود
- جابه‌جایی مرزهای اعتماد

تصمیم با قضاوت Claude است، نه با یک عدد آستانه.

اگر refresh لازم بود، `project-understanding` دوباره اجرا می‌شود و نقشه را بازنویسی می‌کند.

> **این کل مکانیزم است.**
>
> دو خط در سرآمد فایل و یک `git diff`.
> هیچ cache، هیچ state machine، هیچ فایل قفل، هیچ ردیابی خودکار، هیچ زیرساخت.
> اگر روزی این کافی نبود، آن وقت دربارهٔ چیز بیشتری حرف می‌زنیم.

---

## 4. Skill Architecture

```text
skills/
  project-understanding/
    SKILL.md
    references/

  security-review/
    SKILL.md
    references/

  remediation-planner/
    SKILL.md
    references/
```

**همین. فایل دیگری اضافه نمی‌شود مگر با دلیل عملی مشخص.**

- `SKILL.md` — زیر ۳۰۰ خط: نقش، دو بخش `IN SCOPE` و `OUT OF SCOPE`، روال بازرسی،
  قوانین شواهد، قرارداد خروجی
- `references/` — چک‌لیست تخصصی و جزئیات بلند که همیشه لازم نیستند

Claude محتوای `SKILL.md` را همیشه موقع فعال‌شدن می‌خواند، ولی `references/` را فقط
وقتی لازم شود. پس هر چه در `SKILL.md` است باید هر بار ارزش خوانده‌شدن داشته باشد.

### 4.1 قراردادهای مشترک

سه فایل در `shared/` که **مستقیماً خوانده می‌شوند، نه کپی**:

```text
shared/
  findings-schema.md
  trust-model.md
  severity-rubric.md
```

فایل `install.sh` یک symlink ثابت می‌سازد:

```text
~/.claude/skills-shared/  →  <repo>/shared/
```

و هر `SKILL.md` می‌گوید: قبل از نوشتن خروجی، فایل
`~/.claude/skills-shared/findings-schema.md` را بخوان.

یک نسخه، یک محل، بدون همگام‌سازی، بدون اسکریپت.

بسته‌بندی خودکفا برای زمانی است که بخواهیم Skillها را مستقل توزیع کنیم — بخش 15.

---

## 5. Trust Model

**قلب پروژه.** اگر این کار کند، بقیه جزئیات است.

قواعد کامل در [`shared/trust-model.md`](shared/trust-model.md).

### 5.1 چهار سطح اطمینان

| سطح | تعریف | سقف `severity` |
|---|---|---|
| `verified` | کد را خواندم؛ مشکل در خودِ همین کد دیده می‌شود | ندارد |
| `inferred` | کد قویاً نشان می‌دهد، ولی به چیزی وابسته است که ندیدم | `high` |
| `unverified` | ظن است؛ اثباتش به اجرا یا اطلاعاتی که ندارم نیاز دارد | `medium` |
| `unknown` | نتوانستم بررسی کنم | finding نیست |

### 5.2 Evidence اجباری

هیچ findingی بدون این هفت مورد نوشته نمی‌شود:

- `file` — مسیر واقعی و موجود
- `lines` — خط یا بازهٔ واقعی
- `snippet` — عیناً کپی‌شده از فایل؛ نه بازنویسی، نه خلاصه
- `explanation` — چرا این، در همین کد، یک مشکل است
- `impact` — اگر رها شود چه می‌شود و برای چه کسی
- `verification` — دقیقاً چطور می‌شود اثبات یا ردش کرد
- `suggested_fix` — کمترین تغییر ممکن که حلش می‌کند

### 5.3 قوانین ضدتوهم

1. قبل از نوشتن هر finding، خطوطی که به آن استناد می‌کنی را **دوباره بخوان**.
   اگر کد آن چیزی نیست که فکر می‌کردی، finding را **دور بینداز** — اصلاحش نکن.
2. **صفر finding کاملاً قابل قبول است.** هیچ تعداد هدفی وجود ندارد.
   گزارشِ صفر با `coverage` صادقانه، یک گزارش خوب است. پرکردن گزارش شکست است.
3. توصیهٔ عمومی بدون ارجاع به خط مشخص، finding نیست. ننویس.
4. اگر چیزی که ادعایت به آن وابسته است پیدا نشد، `confidence` را پایین بیاور
   و در `verification` بنویس دنبال چه می‌گشتی.
5. «بررسی نشد» را بنویس، نه سکوت. گزارشی که مرزش را نگوید قابل اعتماد نیست.
6. مقدار `severity` از `severity-rubric.md` می‌آید، نه از حس.

### 5.4 خط قرمز مطلق

```text
Fake file  |  Fake line  |  Fake code snippet   =   FAIL
```

شواهد ساختگی تحت **هیچ** شرایطی قابل قبول نیست.
حتی یک مورد یعنی Skill نصب نمی‌شود.

---

## 6. Findings Schema

قرارداد کامل در [`shared/findings-schema.md`](shared/findings-schema.md).

### 6.1 یک منبع حقیقت

هر Reviewer یک فایل تولید می‌کند:

```text
.audit/<skill-name>-findings.json
```

فایل `report.md` تولید نمی‌شود.
دلیل: دو فایل با یک محتوا یعنی دو نسخه که از هم جدا می‌افتند.

Claude بعد از بازرسی، خلاصهٔ فارسی را در ترمینال نمایش می‌دهد.
اگر بعداً واقعاً لازم شد گزارش را برای کسی بفرستی که Claude ندارد، آن وقت دلیلِ عملی
داریم و اضافه‌اش می‌کنیم — بخش 15.

### 6.2 ساختار

```json
{
  "skill": "security-review",
  "repo": "my-project",
  "repo_commit": "a3f91c2",
  "generated_at": "2026-08-16",

  "coverage": {
    "reviewed": ["src/api", "src/auth", "src/db"],
    "file_count": 68,
    "not_reviewed": [
      { "path": "migrations/legacy", "reason": "Superseded by the 2024 rewrite." }
    ],
    "could_not_inspect": [
      { "item": "production config", "reason": "Lives outside the repository." }
    ]
  },

  "checked_and_clean": [
    {
      "area": "Session cookie configuration",
      "file": "src/auth/session.ts",
      "lines": "18-31",
      "note": "httpOnly and secure flags are enabled"
    }
  ],

  "findings": [
    {
      "id": "SEC-001",
      "title": "User-controlled path passed to fs.readFile without normalization",
      "severity": "high",
      "confidence": "verified",
      "status": "open",
      "evidence": [
        {
          "file": "src/api/files.ts",
          "lines": "42-47",
          "snippet": "const p = req.query.path;\nres.send(await fs.readFile(p));"
        }
      ],
      "explanation": "...",
      "impact": "...",
      "verification": "...",
      "suggested_fix": "...",
      "effort": "S",
      "refs": ["CWE-22"]
    }
  ]
}
```

### 6.3 نکات

- مقدار `id` پیشوند دامنه دارد — `SEC-`، `ARCH-`، `DB-` — تا بعد از ادغام قابل ردیابی بماند
- بخش `checked_and_clean` نشان می‌دهد بازرسی جدی بوده، نه اینکه چیزی پیدا نشد چون نگاه نشد.
  هر عضوش فایل و خطِ واقعی دارد و مثل `evidence` اعتبارسنجی می‌شود:
  ادعای «سالم است» هم همان بار اثبات را دارد که یک finding.
- مقدار `status` در ابتدا `open` است و بعداً به `confirmed` / `rejected` / `deferred` / `fixed` می‌رود
- بدنهٔ فنی انگلیسی است چون بعداً به coding agent داده می‌شود؛ خلاصهٔ ترمینال فارسی است

---

## 7. Domain Ownership

هر Reviewer در `SKILL.md` دو بخش صریح دارد:

```markdown
## IN SCOPE
دقیقاً چه چیزی مسئولیت من است

## OUT OF SCOPE
چه چیزی را گزارش نمی‌کنم چون Skill دیگری مسئولش است
```

در Phase 1 با یک Reviewer، همپوشانی کم است — ولی از همین اول رعایت می‌شود،
چون هدفش جلوگیری از finding تکراری بین Skillهاست و بعداً اضافه‌کردنش سخت می‌شود.

### هشدار برای آینده

در لیست اولیهٔ ۱۲ Skill، سه مورد مرز مشخصی ندارند.
اگر همان‌طور ساخته شوند گزارش‌ها به‌شدت تکراری می‌شوند و کار planner را خراب می‌کنند:

- `backend-review` — عمدتاً همان architecture و security و database است، روی یک لایه
- `frontend-review` — همپوشانی سنگین با code-quality و performance
- `ui-ux-review` — بدون اجرای برنامه بیشترش حدس است؛ بالاترین ریسک توهم

تصمیم دربارهٔ این‌ها بعد از اثبات Phase 1 گرفته می‌شود، نه الان.

---

## 8. Remediation Workflow

### 8.1 ورودی planner

همهٔ فایل‌های `*-findings.json` موجود در `.audit/`، به‌علاوهٔ `project-map.md`.

در Phase 1 فقط یکی هست، ولی طراحی از همان اول چندتایی است.

### 8.2 چهار کار planner

1. **ادغام تکراری‌ها** — دو finding با فایل و خطوط هم‌پوشان و علت یکسان یکی می‌شوند؛
   مقادیر `id` اصلی در `merged_from` حفظ می‌شوند
2. **علامت‌زدن تناقض‌ها** — planner **خودش تصمیم نمی‌گیرد**؛
   هر دو دیدگاه را نشان می‌دهد و انتخاب با توست
3. **جداکردن findingهای ضعیف** — هر چیزی با `confidence = unverified` یا evidence ناقص
   به بخش `Needs Verification` می‌رود و وارد صف اصلاح **نمی‌شود**
4. **اولویت‌بندی** — بر اساس `severity × confidence × (1/effort)`، با یک قاعدهٔ سخت:
   هیچ چیزی که `verified` نیست بالای صف نمی‌رود، هر چقدر `severity` بالا باشد

### 8.3 خروجی

فایل `remediation-plan.md` — یک صف مرتب.

هر آیتم یک واحد کار مستقل است با فایل‌های درگیر، اصلاح پیشنهادی، و راه اثبات درستی.

### 8.4 مرحلهٔ اصلاح

**Claude Code معمولی، بدون Skill جدید.**

یک قاعده: یک finding، یک تست، یک commit.
پیام commit شامل `id` است تا ردیابی ممکن بماند.

---

## 9. Evaluation

### 9.1 دو ریپو، نه بیشتر

**ریپو A — intentionally flawed**
پروژهٔ کوچکی با مشکلات مشخصِ کاشته‌شده.
می‌سنجد: آیا مشکلات واقعی را پیدا می‌کند.

**ریپو B — one real project**
یکی از پروژه‌های خودت.
می‌سنجد: usefulness، تعداد false positive، و مقایسه با Claude بدون Skill.

ریپوی تمیز و مجموعهٔ benchmark کامل فعلاً لازم نیست — بخش 15.

### 9.2 دروازهٔ مطلق

```text
Fake file  |  Fake line  |  Fake code snippet   =   FAIL
```

یک مورد کافی است. Skill نصب نمی‌شود، بازنویسی می‌شود.

### 9.3 روش سنجش — بدون هیچ زیرساختی

یک جلسهٔ تازهٔ Claude که Skill را ندیده، فایل findings را می‌گیرد و برای هر finding
سه چیز را چک می‌کند:

1. آیا این فایل و این خطوط وجود دارند؟
2. آیا کد واقعی همان چیزی است که `snippet` می‌گوید؟
3. آیا ادعا از همین کد قابل نتیجه‌گیری است؟

خروجی: `PASS` یا `FAIL` یا `NEEDS-HUMAN`. مورد سوم را خودت نگاه می‌کنی.

### 9.4 مقایسهٔ پایه

همان پرامپت **بدون Skill** هم اجرا می‌شود.

اگر Skill از Claude خالی بهتر نبود، ارزش نصب ندارد — هر چقدر هم خوب نوشته شده باشد.

---

## 10. Repository Structure

```text
claude-skills/
├── BLUEPRINT.md              ← همین سند
├── README.md                 ← نصب و استفاده در ۱۰ خط
├── install.sh
│
├── shared/                   ← مستقیماً خوانده می‌شود
│   ├── findings-schema.md
│   ├── trust-model.md
│   └── severity-rubric.md
│
├── research/                 ← قبل از هر Skill، اجباری
│   ├── project-understanding.md
│   ├── security-review.md
│   └── remediation-planner.md
│
├── skills/
│   ├── project-understanding/
│   │   ├── SKILL.md
│   │   └── references/
│   ├── security-review/
│   │   ├── SKILL.md
│   │   └── references/
│   └── remediation-planner/
│       ├── SKILL.md
│       └── references/
│
└── evals/
    └── fixtures/             ← ریپوی A
```

پوشهٔ `research/` قبل از `skills/` می‌آید — هم در ساختار، هم در زمان.

---

## 11. Installation

```text
<repo>/skills/security-review/   →  ~/.claude/skills/security-review/
<repo>/shared/                   →  ~/.claude/skills-shared/
```

فایل `install.sh` این symlinkها را می‌سازد.
بعد از آن هر `git pull` بلافاصله همه‌جا اعمال می‌شود.

مسیر `~/.claude/skills/` یعنی global — در هر repositoryای در دسترس است.

Skillی که هنوز از Evaluation رد نشده، symlink نمی‌شود.

خروجی‌ها در `.audit/` داخل همان repository نوشته می‌شوند و در `.gitignore` قرار می‌گیرند.
مسیرهای فایل نسبی و درست می‌مانند و چیزی تصادفاً commit نمی‌شود.

---

## 12. Non-Goals

نمی‌سازیم:

- ❌ dashboard
- ❌ orchestration engine
- ❌ runtime
- ❌ agent debate
- ❌ state machine
- ❌ server
- ❌ API
- ❌ database
- ❌ MCP server
- ❌ autonomous fixing system
- ❌ cache یا state machine برای تازگیِ `project-map` — دو خط سرآمد و یک `git diff`
  کافی است، بخش 3.1

**قاعده:** اگر Claude Code خودش می‌تواند، زیرساخت جدید نمی‌سازیم.

---

## 13. Roadmap

| گام | کار | دروازه |
|---|---|---|
| 0 | تأیید همین Blueprint | ✅ تو |
| 1 | اسکلت مخزن و سه فایل `shared/` | ✅ تو |
| 2 | `research/project-understanding.md` | ✅ تو (Research Gate) |
| 3 | ساخت Skill اول | |
| 4 | Evaluation روی ریپوهای A و B | ✅ دروازهٔ مطلق |
| 5 | نصب | ✅ تو |
| 6 | همان چرخه برای `security-review` | ✅ تو |
| 7 | همان چرخه برای `remediation-planner` | ✅ تو |
| 8 | **توقف** — تست کامل روی پروژهٔ واقعی | 🎯 |

بین گام 7 و هر کار بعدی، **توقف اجباری**.

---

## 14. Definition of Done

### 14.1 برای یک Research

همهٔ Skillها:

- [ ] هر تصمیم طراحی به یک منبع مشخص وصل است
- [ ] بخش `What We Reject` خالی نیست
- [ ] تازگی منابع بررسی شده
- [ ] اختلاف بین منابع، اگر بود، تحلیل و ثبت شده
- [ ] هر بخش حذف‌شده دلیل مکتوب دارد
- [ ] هیچ بخشی با متن توخالی پر نشده
- [ ] **تو تأیید کرده‌ای**

Domain Reviewerها، علاوه بر بالا:

- [ ] هر پانزده بخش پر شده
- [ ] حداقل یک منبع ردهٔ 1 تا 3
- [ ] بخش‌های `Common False Positives` و `Common Blind Spots` واقعاً پر شده‌اند

### 14.2 برای یک Skill

- [ ] Research تأییدشده وجود دارد
- [ ] فایل `SKILL.md` زیر ۳۰۰ خط، با `IN SCOPE` و `OUT OF SCOPE`
- [ ] خروجی دقیقاً مطابق `findings-schema.md`
- [ ] روی ریپو A مشکلات کاشته‌شده را پیدا می‌کند
- [ ] روی ریپو B مفید است و false positive کم دارد
- [ ] **صفر شواهد ساختگی**
- [ ] بهتر از Claude بدون Skill بودنش نشان داده شده
- [ ] **تو تأیید کرده‌ای** → نصب

### 14.3 برای Phase 1

- [ ] هر سه Skill قبول و نصب شده
- [ ] یک چرخهٔ کامل واقعی: map ← review ← plan ← fix ← re-review
- [ ] حداقل یک اصلاح واقعیِ commit شده که از این مسیر آمده
- [ ] فایل `README.md` طوری که شش ماه بعد بدون یادآوری قابل استفاده باشد
- [ ] هیچ dashboard، orchestration یا سروری ساخته نشده ✅

---

## 15. Future Enhancements

هیچ‌کدام تا اثبات Phase 1 ساخته نمی‌شوند.

**نسخه‌بندی رسمی**
فایل `VERSION`، فایل `CHANGELOG` برای هر Skill، تگ انتشار، semantic versioning،
و تاریخچهٔ نسخه‌بندی‌شدهٔ evalها.
*فعلاً git history کافی است.*

**بسته‌بندی خودکفا**
کپی قراردادهای مشترک داخل هر Skill از طریق `sync-shared.sh`.
*فقط اگر بخواهیم Skillها را مستقل توزیع کنیم.*

**فایل `report.md`**
گزارش Markdown کنار JSON.
*فقط اگر لازم شد برای کسی بفرستی که Claude ندارد.*

**Evaluation کامل**
ریپوی تمیز، benchmark suite، سنجه‌های عددی، و رهگیری رگرسیون.

**Skillهای بیشتر**
database، architecture، code-quality، performance، QA، DevOps، backend، frontend،
accessibility، UX.
*به ترتیبی که بعد از Phase 1 تصمیم می‌گیریم.*

**Skill `remediation-engineer`**
فقط اگر ثابت شود Claude Code معمولی حلقهٔ «یک finding، یک تست، یک commit» را رعایت نمی‌کند.

**یکپارچگی با CI**
فقط بعد از پایدارشدن Skillها.

---

## خلاصهٔ تغییرات نسبت به نسخهٔ قبل

### What Was Removed From MVP

- **۹ Skill از ۱۲** — ارزش هیچ‌کدام قبل از اثبات سه‌تای اول معلوم نیست
- **`report.md`** — دادهٔ تکراری بدون دلیل عملی؛ یک منبع حقیقت کافی است
- **`templates/` در هر Skill** — شِما در `shared/` است؛ قالب جدا لازم نیست
- **`evals/` داخل هر Skill** — در `evals/` ریشه جمع می‌شود
- **`VERSION` و `CHANGELOG` هر Skill** — git history برای مرحلهٔ آزمایشی کافی است
- **`scripts/sync-shared.sh`** — پوشهٔ `shared/` مستقیماً خوانده می‌شود؛ کپی لازم نیست
- **`references/_shared/` در هر Skill** — همان دلیل
- **ریپوی تمیز (C) در Evaluation** — دو ریپو برای MVP کافی است
- **شش سنجهٔ عددی Evaluation** — جای آن یک دروازهٔ مطلق و قضاوت انسانی نشست
- **`remediation-engineer`** — Claude Code معمولی این کار را می‌کند

### What Was Moved To Future

نسخه‌بندی رسمی · بسته‌بندی خودکفا · `report.md` · Evaluation کامل و benchmark ·
۹ Skill باقی‌مانده · `remediation-engineer` · یکپارچگی CI

جزئیات در بخش 15.

### What Phase 1 Contains

```text
۳ Skill        project-understanding, security-review, remediation-planner
۳ Research     یکی برای هر Skill — قبل از ساخت، با دروازهٔ تأیید
۳ قرارداد      findings-schema, trust-model, severity-rubric
۱ اسکریپت      install.sh (فقط symlink)
۲ ریپوی تست    A: مشکل‌دارِ عمدی · B: پروژهٔ واقعی
۱ خروجی        findings.json + خلاصهٔ فارسی در ترمینال
```

بعد: **توقف** و تست روی پروژهٔ واقعی.

### Research-First Development Rules

**۱.** هیچ Skillی از حافظهٔ مدل ساخته نمی‌شود.

**۲.** اول حوزه مطالعه می‌شود، نه Skillهای مشابه.
سؤال محوری: یک متخصص Senior واقعی، امروز، دقیقاً دنبال چه می‌گردد و چرا؟

**۳.** فایل `research/<skill-name>.md` قبل از Skill وجود دارد.
Domain Reviewerها هر پانزده بخش را پر می‌کنند.
Mapper و Synthesizer فقط بخش‌های مرتبط را، و هر حذف دلیل مکتوب دارد.
هیچ بخشی صرفاً برای پرکردن قالب تولید نمی‌شود.

**۴.** سلسله‌مراتب منابع وزنِ پیش‌فرض است، نه قانون مطلق.
GitHub ردهٔ 7 است، Skillهای موجود ردهٔ 8، و استانداردها و مستندات رسمی ردهٔ 1 تا 3.
وقتی دو منبع معتبر اختلاف دارند، پنج محور `scope` و `applicability` و تاریخ و
`specificity` و `authority` بررسی می‌شود و اختلاف و تصمیم نهایی در Research ثبت می‌شود.

**۵.** ستاره معیار نیست. ده محور بخش 1.5 برای هر repository بررسی می‌شود،
به‌ویژه «آیا AI-generated و توخالی است؟» و «آیا هنوز معتبر است؟».

**۶.** تازگی همیشه بررسی می‌شود: آیا استاندارد یا منبع جدیدتری وجود دارد؟

**۷.** بخش‌های `Common False Positives` و `Common Blind Spots` اجباری‌اند، نه اختیاری.
چیزی که Skill را از یک چک‌لیست جدا می‌کند این است که بداند کجا اشتباه می‌کند.

**۸.** بخش `What We Reject` باید پر باشد.
تحقیقی که چیزی را رد نکرده، تحقیق نکرده — فقط جمع‌آوری کرده.

**۹.** هر محور بازرسی به یک منبع مشخص وصل است.
محورِ بدون منبع یعنی از حافظهٔ مدل آمده، و حذف می‌شود.

**۱۰.** Research ضعیف یعنی Skill ساخته نمی‌شود. بدون استثنا.

---

## وضعیت اجرا

Blueprint در ۲۰۲۶-۰۸-۱۶ تأیید شد.

| گام | وضعیت |
|---|---|
| 0 — Blueprint | ✅ تأیید شد |
| 1 — اسکلت مخزن و `shared/` | ✅ تأیید شد |
| 2 — `research/project-understanding.md` | ✅ تأیید شد |
| 3 — ساخت Skill `project-understanding` | ⏳ در نوبت |

هیچ Skillی ساخته نمی‌شود تا Research مربوطه‌اش تأیید شود.
